#include <iostream>
#include <stdlib.h>
#include <stdio.h>

#include "Triangle.h"

using namespace std;

int main() {
    int c, x, y, length, alfa;
    Triangle* RTr = new Triangle(c, x, y, length, alfa);
}